source "https://rubygems.org"

# GitHub Pages gem (locks all dependencies to GitHub's supported versions)
gem "github-pages", group: :jekyll_plugins

# Plugins used
group :jekyll_plugins do
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end
